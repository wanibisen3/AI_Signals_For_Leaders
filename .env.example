# Server-only secret (used by api/openai.ts on Vercel)
OPENAI_API_KEY=your_openai_api_key_here
# Browser-exposed base URL for the separate backend service (auth/signals routes)
# Local dev example: http://localhost:3001
VITE_BACKEND_URL=http://localhost:3001
